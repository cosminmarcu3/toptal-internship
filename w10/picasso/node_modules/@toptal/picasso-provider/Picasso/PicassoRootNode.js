import React, { forwardRef } from 'react';
import { makeStyles } from '@material-ui/core/styles';
import globalStyles from './styles';
const useGlobalStyles = makeStyles(globalStyles, {
    name: 'Picasso',
});
const PicassoRootNode = forwardRef((props, ref) => {
    const { children } = props;
    const classes = useGlobalStyles();
    return (React.createElement("div", { ref: ref, className: classes.root }, children));
});
PicassoRootNode.displayName = 'PicassoRootNode';
export default PicassoRootNode;
//# sourceMappingURL=PicassoRootNode.js.map