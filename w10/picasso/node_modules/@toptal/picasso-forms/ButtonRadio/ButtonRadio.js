var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useContext } from 'react';
import { Button } from '@toptal/picasso';
import { Field } from 'react-final-form';
import { RadioGroupContext } from '../RadioGroup';
const ButtonRadio = (_a) => {
    var { name } = _a, rest = __rest(_a, ["name"]);
    const groupName = useContext(RadioGroupContext);
    return (
    // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
    React.createElement(Field, { name: name || groupName, type: 'radio', value: rest.value }, ({ input }) => React.createElement(Button.Radio, Object.assign({ checked: input.checked }, rest))));
};
ButtonRadio.displayName = 'ButtonRadio';
export default ButtonRadio;
//# sourceMappingURL=ButtonRadio.js.map