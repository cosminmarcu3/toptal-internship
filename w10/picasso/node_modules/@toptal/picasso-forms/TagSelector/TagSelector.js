var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import { TagSelector as PicassoTagSelector, } from '@toptal/picasso';
import FieldLabel from '../FieldLabel';
import InputField from '../InputField';
export const TagSelector = (props) => {
    const { label, titleCase } = props, rest = __rest(props, ["label", "titleCase"]);
    return (React.createElement(InputField, Object.assign({}, rest, { label: label ? (React.createElement(FieldLabel, { name: props.name, required: props.required, label: label, titleCase: titleCase })) : null }), (inputProps) => {
        const { value } = inputProps, restOfInputProps = __rest(inputProps
        // avoid passing empty string to TagSelector
        , ["value"]);
        // avoid passing empty string to TagSelector
        const valueAsArray = Array.isArray(value) ? value : [];
        return React.createElement(PicassoTagSelector, Object.assign({}, restOfInputProps, { value: valueAsArray }));
    }));
};
TagSelector.displayName = 'TagSelector';
export default TagSelector;
//# sourceMappingURL=TagSelector.js.map