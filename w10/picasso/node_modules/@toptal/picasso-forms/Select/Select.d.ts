import { SelectProps, SelectValueType } from '@toptal/picasso';
import { FieldProps } from '../Field';
export declare type Props<T extends SelectValueType, M extends boolean = false> = SelectProps<T, M> & FieldProps<SelectProps<T, M>['value']>;
export declare const Select: {
    <T extends SelectValueType, M extends boolean = false>(props: Props<T, M>): JSX.Element;
    defaultProps: {};
    displayName: string;
};
export default Select;
