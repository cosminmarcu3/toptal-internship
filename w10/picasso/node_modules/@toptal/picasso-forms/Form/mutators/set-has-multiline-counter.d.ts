import { MutableState } from 'final-form';
export declare const setHasMultilineCounter: <FormValues = object, InitialFormValues = Partial<FormValues>>(args: [name: string, hasCounter: boolean], state: MutableState<FormValues, InitialFormValues>) => void;
