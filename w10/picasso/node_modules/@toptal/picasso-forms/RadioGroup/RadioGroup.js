var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import { Radio as PicassoRadio, } from '@toptal/picasso';
import PicassoField from '../Field';
import FieldLabel from '../FieldLabel';
import RadioGroupContext from './RadioGroupContext';
export const RadioGroup = (props) => {
    const { children, label, titleCase } = props, rest = __rest(props, ["children", "label", "titleCase"]);
    return (React.createElement(RadioGroupContext.Provider, { value: props.name },
        React.createElement(PicassoField, Object.assign({}, rest, { type: 'radio', label: label ? (React.createElement(FieldLabel, { name: props.name, required: props.required, label: label, titleCase: titleCase })) : null }), radioGroupProps => (React.createElement(PicassoRadio.Group, Object.assign({}, radioGroupProps), children)))));
};
RadioGroup.defaultProps = {};
RadioGroup.displayName = 'RadioGroup';
export default RadioGroup;
//# sourceMappingURL=RadioGroup.js.map