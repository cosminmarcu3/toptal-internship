import { ReactNode } from 'react';
import { ButtonProps } from '@toptal/picasso';
import { ButtonCircularProps } from '@toptal/picasso/ButtonCircular';
declare type ButtonTypeProps = {
    buttonType: 'rectangular';
    variant?: ButtonProps['variant'];
    children: ReactNode;
};
declare type CircularButtonTypeProps = {
    buttonType: 'circular';
    variant?: ButtonCircularProps['variant'];
};
declare type ActionButtonTypeProps = {
    buttonType: 'action';
    variant?: null;
    children?: ReactNode;
};
export declare type Props = Omit<ButtonProps, 'type' | 'variant' | 'children'> & {
    id?: string;
} & (CircularButtonTypeProps | ActionButtonTypeProps | ButtonTypeProps);
export declare const SubmitButton: {
    ({ buttonType, variant, ...restOfProps }: Props): JSX.Element;
    defaultProps: {
        buttonType: string;
    };
    displayName: string;
};
export default SubmitButton;
