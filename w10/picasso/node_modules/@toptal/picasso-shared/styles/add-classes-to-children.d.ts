import { ElementType, ReactNode } from 'react';
import { Classes } from './types';
declare type ConfigItem = [ElementType, Classes];
declare type Config = (classes: Classes) => ConfigItem[];
export interface Arguments {
    classes: Classes;
    children: ReactNode;
    config: Config;
}
declare const addClassesToChildren: ({ classes, children, config }: Arguments) => {}[] | null | undefined;
export default addClassesToChildren;
