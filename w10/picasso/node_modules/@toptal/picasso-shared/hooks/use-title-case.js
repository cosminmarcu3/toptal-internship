import { useAppConfig } from '@toptal/picasso-provider';
export const useTitleCase = (componentTitleCase) => {
    const { titleCase: appTitleCase } = useAppConfig();
    return componentTitleCase !== null && componentTitleCase !== void 0 ? componentTitleCase : appTitleCase;
};
//# sourceMappingURL=use-title-case.js.map