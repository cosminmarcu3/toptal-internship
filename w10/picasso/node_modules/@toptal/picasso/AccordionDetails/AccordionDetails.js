var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
import { AccordionDetails as MUIAccordionDetails } from '@material-ui/core';
import styles from './styles';
const useStyles = makeStyles(styles, {
    name: 'PicassoAccordionDetails',
});
const AccordionDetails = (props) => {
    const { children, 
    // Avoid passing external classes inside the rest props
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    classes: externalClasses } = props, rest = __rest(props, ["children", "classes"]);
    const classes = useStyles(props);
    return (React.createElement(MUIAccordionDetails, Object.assign({}, rest, { classes: classes }), children));
};
export default AccordionDetails;
//# sourceMappingURL=AccordionDetails.js.map