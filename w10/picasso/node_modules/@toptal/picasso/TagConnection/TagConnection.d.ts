export declare type Props = {
    /**
     * renders number of connections
     */
    children: string;
};
declare const TagConnection: ({ children }: Props) => JSX.Element;
export default TagConnection;
