import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
import { Link16 } from '../Icon';
import Typography from '../Typography';
import styles from '../Tag/styles';
const useStyles = makeStyles(styles, { name: 'PicassoLabel' });
const TagConnection = ({ children }) => {
    const classes = useStyles();
    return (React.createElement(Typography, { className: classes.connection, color: 'inherit', as: 'span', size: 'xsmall' },
        React.createElement(Link16, null),
        children));
};
export default TagConnection;
//# sourceMappingURL=TagConnection.js.map