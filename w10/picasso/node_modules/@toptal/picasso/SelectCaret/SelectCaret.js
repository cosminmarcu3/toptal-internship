import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
import cx from 'classnames';
import { DropdownArrows16 } from '../Icon';
import styles from './styles';
const useStyles = makeStyles(styles);
const SelectCaret = ({ disabled }) => {
    const classes = useStyles();
    return (React.createElement(DropdownArrows16, { className: cx(classes.caret, {
            [classes.caretDisabled]: disabled,
        }) }));
};
export default SelectCaret;
//# sourceMappingURL=SelectCaret.js.map