import Quill from 'quill';
declare type Props = {
    quill?: Quill;
    defaultValue?: string;
};
declare const useDefaultValue: ({ defaultValue, quill }: Props) => void;
export default useDefaultValue;
