import { FileUpload } from '../FileInput/types';
export interface Props {
    file: FileUpload;
    index: number;
    disabled?: boolean;
    onRemove?: (fileName: string, index: number) => void;
    testIds?: {
        progressBar?: string;
    };
}
declare const FileListItem: ({ file, index, disabled, onRemove, testIds }: Props) => JSX.Element;
export default FileListItem;
