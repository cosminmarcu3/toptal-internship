import React, { forwardRef } from 'react';
import { makeStyles } from '@material-ui/core/styles';
import Typography from '../Typography';
import styles from './styles';
const useStyles = makeStyles(styles, {
    name: 'QuillEditorView',
});
const QuillEditorView = forwardRef(function QuillEditorView({ id, 'data-testid': dataTestId }, ref) {
    const classes = useStyles();
    return (React.createElement(Typography, { as: 'div', variant: 'body', color: 'dark-grey', size: 'medium', className: classes.root, "data-testid": dataTestId, id: id, ref: ref }));
});
export default QuillEditorView;
//# sourceMappingURL=QuillEditorView.js.map