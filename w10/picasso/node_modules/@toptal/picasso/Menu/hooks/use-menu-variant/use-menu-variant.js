import { useContext } from 'react';
import MenuContext from '../../MenuContext';
const useMenuVariant = (props) => {
    const { variant: propsVariant } = props;
    const { variant: contextVariant } = useContext(MenuContext);
    return contextVariant !== null && contextVariant !== void 0 ? contextVariant : propsVariant;
};
export default useMenuVariant;
//# sourceMappingURL=use-menu-variant.js.map