import React, { ReactNode, MouseEvent, HTMLAttributes } from 'react';
import { BaseProps } from '@toptal/picasso-shared';
export interface Props extends BaseProps, HTMLAttributes<HTMLTableRowElement> {
    /** Should be valid `<tr>` children such as `Table.Cell`. */
    children: ReactNode;
    /** If true, the table row will shade on hover */
    hover?: boolean;
    /** If true, the table row will have the selected shading */
    selected?: boolean;
    /** Callback invoked when user clicks on table row */
    onClick?: (event: MouseEvent<HTMLTableRowElement>) => void;
    /** Set a stripe even background for the row */
    stripeEven?: boolean;
}
export declare const TableRow: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLTableRowElement>>;
export default TableRow;
