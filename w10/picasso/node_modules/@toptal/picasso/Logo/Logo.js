import React, { forwardRef } from 'react';
import cx from 'classnames';
import { makeStyles } from '@material-ui/core/styles';
import { Logo as LogoIcon, LogoEmblem as LogoEmblemIcon } from '../Icon';
import styles from './styles';
const useStyles = makeStyles(styles, { name: 'PicassoLogo' });
export const Logo = forwardRef(function Logo(props, ref) {
    const { emblem, variant = 'default', style, className, 'data-testid': testId, } = props;
    const classes = useStyles();
    const rootClass = emblem ? classes.rootEmblem : classes.root;
    const colorClass = classes[variant];
    const LogoComponent = emblem ? LogoEmblemIcon : LogoIcon;
    return (React.createElement(LogoComponent, { ref: ref, className: cx(rootClass, colorClass, className), style: style, "data-testid": testId }));
});
Logo.defaultProps = {
    variant: 'default',
};
Logo.displayName = 'Logo';
export default Logo;
//# sourceMappingURL=Logo.js.map