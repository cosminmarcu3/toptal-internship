var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import { makeStyles } from '@material-ui/core/styles';
import cx from 'classnames';
import Typography from '../Typography';
import styles from './styles';
const useStyles = makeStyles(styles, {
    name: 'PicassoHelpboxContent',
});
export const HelpboxContent = forwardRef(function HelpboxContent(props, ref) {
    const { width, className, style, children } = props, rest = __rest(props, ["width", "className", "style", "children"]);
    const classes = useStyles();
    return (React.createElement(Typography, Object.assign({}, rest, { ref: ref, className: cx({ [classes.shrinkWidth]: width === 'shrink' }, className), style: style, variant: 'body', as: 'div', size: 'medium', color: 'black' }), children));
});
HelpboxContent.defaultProps = {
    width: 'full',
};
HelpboxContent.displayName = 'HelpboxContent';
export default HelpboxContent;
//# sourceMappingURL=HelpboxContent.js.map