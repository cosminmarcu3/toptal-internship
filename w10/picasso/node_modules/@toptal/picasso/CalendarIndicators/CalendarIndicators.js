import React, { useMemo } from 'react';
import { makeStyles } from '@material-ui/core/styles';
import cx from 'classnames';
import styles from './styles';
const useStyles = makeStyles(styles, {
    name: 'PicassoCalendarIndicators',
});
export const CalendarIndicators = ({ date, indicatedIntervals, isSelected, isToday, }) => {
    const classes = useStyles();
    const isIndicated = useMemo(() => {
        return (indicatedIntervals != null &&
            indicatedIntervals.some(({ start, end }) => date >= start && date <= end));
    }, [indicatedIntervals, date]);
    if (isToday || indicatedIntervals) {
        return (React.createElement("div", { className: classes.indicators },
            isToday && (React.createElement("div", { className: cx(classes.today, {
                    [classes.selected]: isSelected,
                }) })),
            isIndicated && React.createElement("div", { className: classes.indicated })));
    }
    return null;
};
//# sourceMappingURL=CalendarIndicators.js.map