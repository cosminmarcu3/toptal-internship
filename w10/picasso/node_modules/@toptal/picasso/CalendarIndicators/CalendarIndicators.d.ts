export interface Props {
    indicatedIntervals?: {
        start: Date;
        end: Date;
    }[];
    date: Date;
    isSelected: boolean;
    isToday: boolean;
}
export declare const CalendarIndicators: ({ date, indicatedIntervals, isSelected, isToday, }: Props) => JSX.Element | null;
