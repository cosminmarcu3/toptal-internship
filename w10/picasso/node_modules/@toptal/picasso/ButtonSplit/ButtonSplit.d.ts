import React, { ReactNode, HTMLAttributes } from 'react';
import { BaseProps } from '@toptal/picasso-shared';
import { ButtonProps } from '../Button';
export interface Props extends BaseProps, Omit<HTMLAttributes<HTMLDivElement>, 'onClick'> {
    /** Content of Button component */
    children: ReactNode;
    /** Content element that opens when anchor is clicked */
    menu: ReactNode;
    /** Callback invoked when component is clicked */
    onClick?: ButtonProps['onClick'];
    /** A button can have different sizes */
    size?: ButtonProps['size'];
    /** The variant to use */
    variant?: 'primary' | 'secondary';
    /** Is component disaled or not */
    disabled?: boolean;
    menuButtonProps?: Omit<ButtonProps, 'children'>;
    actionButtonProps?: Omit<ButtonProps, 'children'>;
    testIds?: {
        actionButton?: string;
        menuButton?: string;
    };
}
export declare const ButtonSplit: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLDivElement>>;
export default ButtonSplit;
