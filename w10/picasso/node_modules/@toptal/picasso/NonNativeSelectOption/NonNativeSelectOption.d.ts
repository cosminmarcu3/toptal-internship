import React, { ReactNode } from 'react';
import { ItemProps, Option, ValueType } from '../SelectBase';
export interface Props<T extends ValueType> extends ItemProps {
    children?: ReactNode;
    description?: ReactNode;
    selected: boolean;
    highlighted: boolean;
    option: Option<T>;
}
declare const NonNativeSelectOption: React.MemoExoticComponent<(<T extends ValueType>({ option, selected, highlighted, description, children, ...itemProps }: Props<T>) => JSX.Element)>;
export default NonNativeSelectOption;
