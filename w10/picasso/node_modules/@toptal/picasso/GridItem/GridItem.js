var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import { makeStyles } from '@material-ui/core/styles';
import { Grid as MUIGrid } from '@material-ui/core';
import styles from './styles';
const useStyles = makeStyles(styles, {
    name: 'PicassoGridItem',
});
export const GridItem = forwardRef(function GridItem(props, ref) {
    const { children, small, medium, large, className, style } = props, rest = __rest(props, ["children", "small", "medium", "large", "className", "style"]);
    const classes = useStyles();
    return (React.createElement(MUIGrid, Object.assign({}, rest, { ref: ref, item: true, lg: large, md: medium, xs: small, classes: classes, className: className, style: style }), children));
});
GridItem.defaultProps = {};
GridItem.displayName = 'GridItem';
export default GridItem;
//# sourceMappingURL=GridItem.js.map