import React, { forwardRef } from 'react';
import TimelineRow from '../TimelineRow';
import Container from '../Container';
export const Timeline = forwardRef(function Timeline({ className, children }, ref) {
    return (React.createElement(Container, { ref: ref, className: className, flex: true, direction: 'column' }, children));
});
Timeline.displayName = 'Timeline';
export default Object.assign(Timeline, { Row: TimelineRow });
//# sourceMappingURL=Timeline.js.map