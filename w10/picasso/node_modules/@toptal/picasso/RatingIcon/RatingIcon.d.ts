import React from 'react';
import { SizeType } from '@toptal/picasso-shared';
export interface Props {
    active: boolean;
    hovered?: boolean;
    interactive: boolean;
    size: SizeType<'small' | 'large'>;
}
declare const RatingIcon: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLDivElement>>;
export default RatingIcon;
