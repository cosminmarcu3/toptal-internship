import { ButtonControlLabelProps } from '../ButtonControlLabel';
export interface Props extends Omit<ButtonControlLabelProps, 'control'> {
    testIds?: {
        radio?: string;
    };
}
declare const ButtonRadio: ({ testIds, ...rest }: Props) => JSX.Element;
export default ButtonRadio;
