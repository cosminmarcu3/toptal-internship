import React, { ReactNode, HTMLAttributes } from 'react';
import { BaseProps } from '@toptal/picasso-shared';
export interface Props extends BaseProps, HTMLAttributes<HTMLTableSectionElement> {
    /** Zero or more Table.Row elements. */
    children: ReactNode;
}
export declare const TableFooter: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLTableSectionElement>>;
export default TableFooter;
