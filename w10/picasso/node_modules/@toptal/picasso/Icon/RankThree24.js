import React, { forwardRef } from 'react';
import cx from 'classnames';
import { makeStyles } from '@material-ui/core/styles';
import kebabToCamelCase from '../utils/kebab-to-camel-case';
import styles from './styles';
const BASE_SIZE = 24;
const useStyles = makeStyles(styles, {
    name: 'PicassoSvgRankThree24',
});
const SvgRankThree24 = forwardRef(function SvgRankThree24(props, ref) {
    const { className, style = {}, color, scale, base, 'data-testid': testId, } = props;
    const classes = useStyles(props);
    const classNames = [classes.root, className];
    const scaledSize = base || BASE_SIZE * Math.ceil(scale || 1);
    const colorClassName = kebabToCamelCase(`${color}`);
    if (classes[colorClassName]) {
        classNames.push(classes[colorClassName]);
    }
    const svgStyle = Object.assign({ minWidth: `${scaledSize}px`, minHeight: `${scaledSize}px` }, style);
    return (React.createElement("svg", { viewBox: '0 0 24 24', className: cx(...classNames), style: svgStyle, ref: ref, "data-testid": testId },
        React.createElement("path", { d: 'm12 13.86 8.203 6.562-.625.78L12 15.14l-7.578 6.063-.625-.781L12 13.86Zm0-6 8.203 6.562-.625.78L12 9.14l-7.578 6.063-.625-.781L12 7.86Zm0-6 8.203 6.562-.625.78L12 3.14 4.422 9.203l-.625-.781L12 1.86Z' })));
});
SvgRankThree24.displayName = 'SvgRankThree24';
export default SvgRankThree24;
//# sourceMappingURL=RankThree24.js.map