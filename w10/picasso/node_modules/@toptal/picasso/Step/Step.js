var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
import { Step as MUIStep } from '@material-ui/core';
import styles from './styles';
const useStyles = makeStyles(styles, { name: 'PicassoStep' });
export const Step = (props) => {
    const { active, children, completed } = props, rest = __rest(props, ["active", "children", "completed"]);
    const classes = useStyles();
    return (React.createElement(MUIStep, Object.assign({}, rest, { active: active, completed: completed, classes: classes }), children));
};
Step.displayName = 'Step';
export default Step;
//# sourceMappingURL=Step.js.map