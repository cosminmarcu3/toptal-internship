import { AutocompleteProps } from '../Autocomplete';
export interface Props extends AutocompleteProps {
    /** The variant to use */
    variant?: 'light' | 'dark';
    testIds?: AutocompleteProps['testIds'];
}
export declare const PageAutocomplete: {
    ({ variant, testIds, ...rest }: Props): JSX.Element;
    displayName: string;
    defaultProps: {
        variant: string;
    };
};
export default PageAutocomplete;
