import React, { HTMLAttributes } from 'react';
import { BaseProps, TextLabelProps } from '@toptal/picasso-shared';
import '../StepIcon';
export interface StepperBaseProps extends BaseProps, TextLabelProps, HTMLAttributes<HTMLDivElement> {
    /** The index of the active step */
    active?: number;
    /** Array of the step labels */
    steps: string[];
    /** Enable overflow ellipsis for labels */
    overflowEllipsis?: boolean;
}
export interface Props extends StepperBaseProps {
    /** Hide labels of non active steps */
    hideLabels?: boolean;
    direction?: 'vertical' | 'horizontal';
}
declare const Stepper: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLDivElement>>;
export default Stepper;
