import { useCallback, useContext, useEffect } from 'react';
import MenuContext from '../../../Menu/MenuContext';
const useSlideMenuItem = (props) => {
    const { key, menu, onClick } = props;
    const { onItemClick, onItemUpdate } = useContext(MenuContext);
    useEffect(() => {
        if (onItemUpdate) {
            onItemUpdate(key, menu);
        }
    }, [key, menu, onItemUpdate]);
    const handleItemClick = useCallback((event) => {
        if (menu && onItemClick) {
            event.stopPropagation();
            onItemClick(key, menu);
        }
        if (onClick) {
            onClick(event);
        }
    }, [key, menu, onClick, onItemClick]);
    return {
        onItemClick: handleItemClick,
    };
};
export default useSlideMenuItem;
//# sourceMappingURL=use-slide-menu-item.js.map