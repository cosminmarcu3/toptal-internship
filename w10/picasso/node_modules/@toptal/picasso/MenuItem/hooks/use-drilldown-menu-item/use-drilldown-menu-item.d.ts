import React, { ReactElement } from 'react';
export interface Props {
    key: string;
    menu?: ReactElement;
    onMouseEnter?: (event: React.MouseEvent<HTMLElement, MouseEvent>) => void;
}
declare const useDrilldownMenuItem: (props: Props) => {
    isOpened: boolean;
    onItemMouseEnter: (event: React.MouseEvent<HTMLElement, MouseEvent>) => void;
    onAwayClick: () => void;
};
export default useDrilldownMenuItem;
