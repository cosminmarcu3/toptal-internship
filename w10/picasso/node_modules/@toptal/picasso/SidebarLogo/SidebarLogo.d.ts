import React, { ReactNode, HTMLAttributes } from 'react';
import { BaseProps } from '@toptal/picasso-shared';
export interface Props extends BaseProps, HTMLAttributes<HTMLDivElement | HTMLSpanElement> {
    children?: ReactNode;
    /** Logo to display when Sidebar is in collapsed state */
    collapsedLogo?: ReactNode;
    /** Logo to display when Sidebar is in default state */
    fullLogo?: ReactNode;
}
export declare const SidebarLogo: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLDivElement>>;
export default SidebarLogo;
