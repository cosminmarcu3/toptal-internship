var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import { makeStyles } from '@material-ui/core';
import cx from 'classnames';
import Container from '../Container';
import styles from './styles';
import { useSidebarContext } from '../PageSidebar/SidebarContextProvider';
const useStyles = makeStyles(styles, {
    name: 'PicassoSidebarLogo',
});
export const SidebarLogo = forwardRef(function SidebarLogo(_a, ref) {
    var { children, collapsedLogo, fullLogo, className } = _a, rest = __rest(_a, ["children", "collapsedLogo", "fullLogo", "className"]);
    const classes = useStyles();
    const { isCollapsed: isSidebarCollapsed } = useSidebarContext();
    const logo = isSidebarCollapsed ? collapsedLogo : fullLogo;
    return (React.createElement(Container, Object.assign({}, rest, { className: cx(classes.root, className), ref: ref, flex: true, bottom: 'xsmall', left: 'large', alignItems: 'center' }),
        logo,
        children));
});
SidebarLogo.defaultProps = {};
SidebarLogo.displayName = 'SidebarLogo';
export default SidebarLogo;
//# sourceMappingURL=SidebarLogo.js.map