import React from 'react';
import { makeStyles } from '@material-ui/core';
import cx from 'classnames';
import Button from '../Button';
import styles from './styles';
const useStyles = makeStyles(styles, {
    name: 'PicassoPaginationButton',
});
const PaginationButton = (props) => {
    const { page, activePage, disabled, onClick, className } = props;
    const classes = useStyles();
    return (React.createElement(Button, { className: cx(classes.root, { [classes.active]: page === activePage }, className), active: activePage === page, disabled: disabled, onClick: () => onClick(page), variant: 'secondary', size: 'small' }, page));
};
export default PaginationButton;
//# sourceMappingURL=PaginationButton.js.map