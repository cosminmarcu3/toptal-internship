var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import { makeStyles } from '@material-ui/core/styles';
import cx from 'classnames';
import Container from '../Container';
import { CloseMinor16 } from '../Icon';
import ButtonCircular from '../ButtonCircular';
import styles from './styles';
import HelpboxContext from './HelpboxContext';
const useStyles = makeStyles(styles, {
    name: 'PicassoHelpbox',
});
// eslint-disable-next-line react/display-name
export const Helpbox = forwardRef(function Helpbox(props, ref) {
    const { className, style, children, variant, onClose } = props, rest = __rest(props, ["className", "style", "children", "variant", "onClose"]);
    const classes = useStyles();
    return (React.createElement(Container, Object.assign({}, rest, { ref: ref, rounded: true, className: cx(classes.root, className), style: style, bordered: !variant || variant === 'white', variant: variant, padded: 'medium' }),
        React.createElement(HelpboxContext.Provider, { value: { closeable: Boolean(onClose) } }, children),
        onClose && (React.createElement(ButtonCircular, { className: classes.closeButton, onClick: onClose, icon: React.createElement(CloseMinor16, { color: 'dark-grey' }) }))));
});
Helpbox.displayName = 'Helpbox';
export default Helpbox;
//# sourceMappingURL=Helpbox.js.map