interface Args {
    activePage: number;
    siblingCount: number;
    totalPages: number;
}
export declare const ELLIPSIS = "...";
declare const getRange: ({ activePage, siblingCount, totalPages }: Args) => (string | number)[];
export default getRange;
