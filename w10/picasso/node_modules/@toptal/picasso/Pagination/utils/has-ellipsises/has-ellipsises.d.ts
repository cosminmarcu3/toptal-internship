interface Args {
    activePage: number;
    totalPages: number;
    siblingCount: number;
}
declare const hasEllipses: ({ activePage, totalPages, siblingCount, }: Args) => [boolean, boolean];
export default hasEllipses;
