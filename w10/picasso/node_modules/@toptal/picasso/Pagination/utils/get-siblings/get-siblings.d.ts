interface Args {
    activePage: number;
    siblingCount: number;
    totalPages: number;
}
declare const getSiblings: ({ activePage, siblingCount, totalPages, }: Args) => [number[], number[]];
export default getSiblings;
