import React, { ReactNode } from 'react';
import { BaseProps } from '@toptal/picasso-shared';
declare type VariantType = 'white' | 'red';
declare type SizeType = 'medium' | 'small' | 'large';
export interface Props extends BaseProps {
    /** The `Badge` content */
    content: number;
    /** Variant of the `Badge` */
    variant?: VariantType;
    /** Size of the `Badge` */
    size?: SizeType;
    /** Max count to show. By default 9 for small size, 99 for other sizes */
    max?: number;
    /** The badged will be overlaid on it's children */
    children?: ReactNode;
}
export declare const Badge: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLDivElement>>;
export default Badge;
