var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import { makeStyles } from '@material-ui/core/styles';
import cx from 'classnames';
import Typography from '../Typography';
import styles from './styles';
const useStyles = makeStyles(styles);
export const NoteTitle = forwardRef(function NoteTitle(_a, ref) {
    var { children, className } = _a, rest = __rest(_a, ["children", "className"]);
    const classes = useStyles();
    return (React.createElement("div", Object.assign({ ref: ref, className: cx(classes.root, className) }, rest),
        React.createElement(Typography, { variant: 'heading', size: 'small' }, children)));
});
NoteTitle.defaultProps = {};
NoteTitle.displayName = 'NoteTitle';
export default NoteTitle;
//# sourceMappingURL=NoteTitle.js.map