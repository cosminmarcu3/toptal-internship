import { DirectionsType, DynamicPointLink, DynamicPointNode, TreeNodeInterface, TreeViewVariant } from './types';
export interface UseTreeArguments {
    data: TreeNodeInterface;
    nodeWidth: number;
    nodeHeight: number;
    direction: DirectionsType;
    verticalMargin: number;
    horizontalMargin: number;
    variant: TreeViewVariant;
}
export interface UseTreeResponse {
    nodes: DynamicPointNode[];
    links: DynamicPointLink[];
    selectedNode: DynamicPointNode | undefined;
}
export declare const useTree: ({ data, nodeWidth, nodeHeight, direction, verticalMargin, horizontalMargin, variant, }: UseTreeArguments) => UseTreeResponse;
