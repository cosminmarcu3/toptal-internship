import { ZoomBehavior, ZoomedElementBaseType } from 'd3-zoom';
import { RefObject } from 'react';
export interface UseZoomArguments<ZoomRefElement extends ZoomedElementBaseType> {
    rootRef: RefObject<ZoomRefElement>;
    scaleExtent: [number, number];
    center?: {
        x: number;
        y: number;
    };
    initialScale?: number;
    transitionDuration: number;
}
export interface UseZoomResponse<ZoomRefElement extends ZoomedElementBaseType> {
    zoom: ZoomBehavior<ZoomRefElement, unknown>;
    handleZoom: (step: number) => void;
}
export declare const useZoom: <ZoomRefElement extends Element>({ rootRef, scaleExtent, center, initialScale, transitionDuration, }: UseZoomArguments<ZoomRefElement>) => UseZoomResponse<ZoomRefElement>;
