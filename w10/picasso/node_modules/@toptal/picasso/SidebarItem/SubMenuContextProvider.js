import React, { createContext, useContext } from 'react';
const Context = createContext({
    isSubMenu: false,
    parentMenu: null,
});
export const SubMenuContextProvider = ({ children, parentMenu, parentSidebarItemIndex, }) => (React.createElement(Context.Provider, { value: {
        isSubMenu: true,
        parentSidebarItemIndex,
        parentMenu,
    } }, children));
export const useSubMenuContext = () => useContext(Context);
//# sourceMappingURL=SubMenuContextProvider.js.map