import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
import cx from 'classnames';
import styles from './styles';
import Container from '../Container';
import Typography from '../Typography';
const useStyles = makeStyles(styles, {
    name: 'PicassoTimelineRow',
});
const TimelineRow = ({ className, children, icon, date, hasConnector, 'data-testid': dataTestId, testIds = {}, }) => {
    const classes = useStyles();
    return (React.createElement(Container, { "data-testid": dataTestId, className: cx(classes.root, className), flex: true },
        React.createElement(Container, { flex: true, direction: 'column', alignItems: 'center', right: 'medium' },
            typeof icon !== 'undefined' ? (React.cloneElement(icon, {
                className: cx(icon.props.className, classes.icon),
            })) : (React.createElement("div", { className: classes.dot, "data-testid": testIds.dot })),
            hasConnector && (React.createElement("div", { className: classes.connector, "data-testid": testIds.connector }))),
        date && (React.createElement(Container, { className: classes.date, right: 'large' },
            React.createElement(Typography, { className: classes.dateText, weight: 'semibold', size: 'medium' }, date))),
        React.createElement(Container, { className: classes.content, bottom: 'large' }, children)));
};
TimelineRow.defaultProps = {
    hasConnector: true,
};
TimelineRow.displayName = 'TimelineRow';
export default TimelineRow;
//# sourceMappingURL=TimelineRow.js.map