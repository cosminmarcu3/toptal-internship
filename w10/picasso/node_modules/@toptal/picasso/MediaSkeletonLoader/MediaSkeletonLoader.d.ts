import React from 'react';
import { BaseProps } from '@toptal/picasso-shared';
interface ImageProps extends BaseProps {
    /** Each variant exposes a different set of props */
    variant: 'image';
    width: string | number;
    height: string | number;
    circle?: boolean;
}
interface AvatarProps extends BaseProps {
    variant: 'avatar';
    size?: 'xxsmall' | 'xsmall' | 'small' | 'medium' | 'large';
}
interface IconProps extends BaseProps {
    variant: 'icon';
    circle?: boolean;
    size?: 'medium' | 'large';
}
export declare type Props = ImageProps | AvatarProps | IconProps;
interface LoaderAttributes {
    width: number;
    height: number;
    borderRadius: string;
}
export declare const getAvatarAttributes: ({ size, }: Omit<AvatarProps, 'variant'>) => LoaderAttributes;
export declare const getIconAttributes: ({ size, circle, }: Omit<IconProps, 'variant'>) => LoaderAttributes;
export declare const getImageAttributes: ({ circle, width, height, }: Omit<ImageProps, 'variant'>) => LoaderAttributes;
export declare const getAttributes: (props: React.PropsWithChildren<Props>) => LoaderAttributes;
export declare const MediaSkeletonLoader: {
    (props: Props): JSX.Element;
    displayName: string;
    defaultProps: {
        variant: string;
    };
};
export default MediaSkeletonLoader;
