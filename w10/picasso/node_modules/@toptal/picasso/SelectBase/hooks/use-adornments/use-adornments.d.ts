import { ReactNode } from 'react';
export interface Props {
    disabled?: boolean;
    position: 'start' | 'end';
    icon?: ReactNode;
    loading?: boolean;
}
export declare const useAdornments: ({ disabled, position, icon, loading }: Props) => (JSX.Element | null)[];
export default useAdornments;
