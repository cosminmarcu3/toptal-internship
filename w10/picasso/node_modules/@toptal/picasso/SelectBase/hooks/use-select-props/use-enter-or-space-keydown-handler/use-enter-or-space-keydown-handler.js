import { useCallback } from 'react';
import flattenOptions from '../../../utils/flatten-options';
const useEnterOrSpaceKeyDownHandler = ({ selectState: { canOpen, open, filteredOptions, highlightedIndex, closeOnEnter, close, }, handleSelect, }) => useCallback((event) => {
    event.preventDefault();
    if (canOpen) {
        open();
        return;
    }
    const item = flattenOptions(filteredOptions)[highlightedIndex];
    if (!item) {
        return;
    }
    if (closeOnEnter) {
        close();
    }
    handleSelect(event, item);
}, [
    canOpen,
    filteredOptions,
    highlightedIndex,
    closeOnEnter,
    handleSelect,
    open,
    close,
]);
export default useEnterOrSpaceKeyDownHandler;
//# sourceMappingURL=use-enter-or-space-keydown-handler.js.map