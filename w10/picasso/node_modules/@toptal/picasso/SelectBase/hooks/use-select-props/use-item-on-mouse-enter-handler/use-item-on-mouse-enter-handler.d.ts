import { UseSelectProps, ValueType } from '../../../types';
declare const useItemOnMouseEnterHandler: <T extends ValueType, M extends boolean = false>({ selectState: { highlightedIndex, setHighlightedIndex }, }: UseSelectProps<T, M, M extends true ? T[] : T>) => (index: number) => void;
export default useItemOnMouseEnterHandler;
