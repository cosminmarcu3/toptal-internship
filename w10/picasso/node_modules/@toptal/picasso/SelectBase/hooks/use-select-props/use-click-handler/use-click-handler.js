import { useCallback } from 'react';
import { EMPTY_INPUT_VALUE } from '../../../utils';
const useClickHandler = ({ selectState: { isOpen, canOpen, open, close, setFilterOptionsValue }, }) => useCallback(() => {
    if (canOpen) {
        setFilterOptionsValue(EMPTY_INPUT_VALUE);
        open();
    }
    else if (isOpen) {
        close();
    }
}, [isOpen, canOpen, open, close, setFilterOptionsValue]);
export default useClickHandler;
//# sourceMappingURL=use-click-handler.js.map