import { ValueType, UseSelectProps } from '../../../types';
declare const useBlurHandler: <T extends ValueType, M extends boolean = false>({ popperRef, selectState: { close, setFilterOptionsValue }, selectProps: { onBlur }, }: UseSelectProps<T, M, M extends true ? T[] : T>) => (event: React.FocusEvent<HTMLInputElement>) => void;
export default useBlurHandler;
