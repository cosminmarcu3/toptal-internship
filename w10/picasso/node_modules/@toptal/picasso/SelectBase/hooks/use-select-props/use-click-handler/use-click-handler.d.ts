import { ValueType, UseSelectProps } from '../../../types';
declare const useClickHandler: <T extends ValueType, M extends boolean = false>({ selectState: { isOpen, canOpen, open, close, setFilterOptionsValue }, }: UseSelectProps<T, M, M extends true ? T[] : T>) => () => void;
export default useClickHandler;
