import { useCallback } from 'react';
const useItemOnClick = ({ selectState: { close }, selectProps: { multiple }, handleSelect, }) => useCallback((event, item) => {
    if (!multiple) {
        close();
    }
    handleSelect(event, item);
}, [close, handleSelect, multiple]);
export default useItemOnClick;
//# sourceMappingURL=use-item-on-click-handler.js.map