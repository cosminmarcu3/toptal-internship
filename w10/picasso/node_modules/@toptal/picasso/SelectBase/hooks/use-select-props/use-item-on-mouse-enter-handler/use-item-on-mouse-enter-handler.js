import { useCallback } from 'react';
const useItemOnMouseEnterHandler = ({ selectState: { highlightedIndex, setHighlightedIndex }, }) => useCallback((index) => {
    if (index === highlightedIndex) {
        return;
    }
    setHighlightedIndex(index);
}, [highlightedIndex, setHighlightedIndex]);
export default useItemOnMouseEnterHandler;
//# sourceMappingURL=use-item-on-mouse-enter-handler.js.map