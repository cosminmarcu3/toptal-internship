import { KeyboardEvent } from 'react';
import { ValueType, UseSelectProps } from '../../../types';
import useSelectHandler from '../use-select-handler';
declare const useEnterOrSpaceKeyDownHandler: <T extends ValueType, M extends boolean = false>({ selectState: { canOpen, open, filteredOptions, highlightedIndex, closeOnEnter, close, }, handleSelect, }: UseSelectProps<T, M, M extends true ? T[] : T> & {
    handleSelect: ReturnType<typeof useSelectHandler>;
}) => (event: KeyboardEvent<HTMLInputElement>) => void;
export default useEnterOrSpaceKeyDownHandler;
