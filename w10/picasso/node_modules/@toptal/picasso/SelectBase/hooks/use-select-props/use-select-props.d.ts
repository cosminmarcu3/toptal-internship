import { ValueType, UseSelectProps, UseSelectOutput } from '../../types';
declare const useSelectProps: <T extends ValueType, M extends boolean = false>(props: UseSelectProps<T, M, M extends true ? T[] : T>) => UseSelectOutput;
export default useSelectProps;
