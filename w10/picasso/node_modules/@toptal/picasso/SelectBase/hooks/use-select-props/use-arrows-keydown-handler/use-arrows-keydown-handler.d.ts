import { KeyboardEvent } from 'react';
import { ValueType, UseSelectProps } from '../../../types';
declare const useArrowsKeyDownHandler: <T extends ValueType, M extends boolean = false>({ selectState: { isOpen, highlightedIndex, filteredOptions, setHighlightedIndex, open, }, }: UseSelectProps<T, M, M extends true ? T[] : T>) => (key: string, event: KeyboardEvent<HTMLInputElement>) => void;
export default useArrowsKeyDownHandler;
