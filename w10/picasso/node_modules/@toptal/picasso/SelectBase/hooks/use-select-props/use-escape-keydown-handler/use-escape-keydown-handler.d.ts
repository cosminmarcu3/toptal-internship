import { KeyboardEvent } from 'react';
import { ValueType, UseSelectProps } from '../../../types';
declare const useEscapeKeyDownHandler: <T extends ValueType, M extends boolean = false>({ selectState: { close }, }: UseSelectProps<T, M, M extends true ? T[] : T>) => (event: KeyboardEvent<HTMLInputElement>) => void;
export default useEscapeKeyDownHandler;
