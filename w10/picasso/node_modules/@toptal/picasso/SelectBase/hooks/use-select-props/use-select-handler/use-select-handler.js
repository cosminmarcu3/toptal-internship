/* eslint-disable max-lines */
import { useCallback } from 'react';
import { EMPTY_INPUT_VALUE, toggleMultipleSelectValue, focusRef, fireOnChangeEvent, } from '../../../utils';
const useSelectHandler = ({ selectState: { emptySelectValue, setFilterOptionsValue }, selectProps: { multiple, value, name, onChange }, selectRef, }) => useCallback((event, option) => {
    let newValue;
    if (option === null) {
        newValue = emptySelectValue;
    }
    else if (multiple) {
        newValue = toggleMultipleSelectValue((value !== null && value !== void 0 ? value : []), option);
    }
    else {
        newValue = option.value;
    }
    fireOnChangeEvent({ event, value: newValue, name, onChange });
    setFilterOptionsValue(EMPTY_INPUT_VALUE);
    focusRef(selectRef);
}, [
    name,
    onChange,
    emptySelectValue,
    setFilterOptionsValue,
    multiple,
    value,
    selectRef,
]);
export default useSelectHandler;
//# sourceMappingURL=use-select-handler.js.map