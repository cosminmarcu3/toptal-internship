import { Option, ValueType, UseSelectProps } from '../../../types';
declare const useSelectHandler: <T extends ValueType, M extends boolean = false>({ selectState: { emptySelectValue, setFilterOptionsValue }, selectProps: { multiple, value, name, onChange }, selectRef, }: UseSelectProps<T, M, M extends true ? T[] : T>) => (event: React.SyntheticEvent, option: Option | null) => void;
export default useSelectHandler;
