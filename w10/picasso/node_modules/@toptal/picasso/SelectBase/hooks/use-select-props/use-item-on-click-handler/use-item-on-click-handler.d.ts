import { UseSelectProps, ValueType, Option } from '../../../types';
import useSelectHandler from '../use-select-handler';
declare const useItemOnClick: <T extends ValueType, M extends boolean = false>({ selectState: { close }, selectProps: { multiple }, handleSelect, }: UseSelectProps<T, M, M extends true ? T[] : T> & {
    handleSelect: ReturnType<typeof useSelectHandler>;
}) => (event: React.MouseEvent, item: Option) => void;
export default useItemOnClick;
