import { ChangeEvent } from 'react';
import { ValueType, UseSelectProps } from '../../../types';
declare const useSearchChangeHandler: <T extends ValueType, M extends boolean = false>({ selectState: { setFilterOptionsValue }, }: UseSelectProps<T, M, M extends true ? T[] : T>) => (event: ChangeEvent<HTMLInputElement>) => void;
export default useSearchChangeHandler;
