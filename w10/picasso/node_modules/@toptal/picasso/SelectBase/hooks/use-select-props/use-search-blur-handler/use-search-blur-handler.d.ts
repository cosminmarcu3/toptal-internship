import { ValueType, UseSelectProps } from '../../../types';
declare const useSearchBlurHandler: <T extends ValueType, M extends boolean = false>({ selectRef, popperRef, selectState: { close }, }: UseSelectProps<T, M, M extends true ? T[] : T>) => (event: React.FocusEvent<HTMLInputElement>) => void;
export default useSearchBlurHandler;
