import { ValueType, UseSelectProps } from '../../../types';
declare const useFocusHandler: <T extends ValueType, M extends boolean = false>({ selectState: { isOpen, setFilterOptionsValue }, }: UseSelectProps<T, M, M extends true ? T[] : T>) => () => void;
export default useFocusHandler;
