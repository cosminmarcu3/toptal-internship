import { useCallback } from 'react';
const useResetClickHandler = ({ selectState: { close }, handleSelect, }) => useCallback((event) => {
    // keep select options closed
    event.stopPropagation();
    close();
    handleSelect(event, null);
}, [close, handleSelect]);
export default useResetClickHandler;
//# sourceMappingURL=use-reset-click-handler.js.map