import isOptionsType from '../../utils/is-options-type';
const flattenOptions = (options) => {
    if (isOptionsType(options)) {
        return options;
    }
    return Object.values(options).reduce((result, optionList) => result.concat(optionList), []);
};
export default flattenOptions;
//# sourceMappingURL=flatten-options.js.map