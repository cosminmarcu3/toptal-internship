import { Option, OptionGroups } from '../../types';
interface Props {
    options: Option[] | OptionGroups;
    limit: number;
}
declare const limitOptions: ({ options, limit }: Props) => Option[] | OptionGroups;
export default limitOptions;
