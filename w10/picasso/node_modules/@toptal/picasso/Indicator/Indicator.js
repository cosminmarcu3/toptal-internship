var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { forwardRef } from 'react';
import cx from 'classnames';
import { makeStyles } from '@material-ui/core/styles';
import styles from './styles';
const useStyles = makeStyles(styles, { name: 'PicassoIndicator' });
export const Indicator = forwardRef(function Indicator(props, ref) {
    const { className, color, style } = props, restProps = __rest(props, ["className", "color", "style"]);
    const classes = useStyles();
    return (React.createElement("div", Object.assign({ role: 'img' }, restProps, { className: cx(classes.root, className, classes[color]), style: style, ref: ref })));
});
Indicator.displayName = 'Indicator';
export default Indicator;
//# sourceMappingURL=Indicator.js.map