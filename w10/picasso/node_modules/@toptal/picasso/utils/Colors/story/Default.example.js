import React, { Fragment } from 'react';
import { palette } from '@toptal/picasso/utils';
import { Grid, Paper, Typography, Container } from '@toptal/picasso';
const colorGroups = Object.entries(palette);
const Example = () => (React.createElement(React.Fragment, null, colorGroups.map(([colorGroupName, colorGroup]) => (React.createElement(Fragment, { key: colorGroupName },
    React.createElement(Container, { top: 2, bottom: 1 },
        React.createElement(Typography, { variant: 'heading', size: 'large' }, colorGroupName)),
    React.createElement(ColorGroup, { colors: Object.entries(colorGroup), colorGroupName: colorGroupName }))))));
const ColorGroup = ({ colors, colorGroupName, }) => (React.createElement(Grid, { spacing: 16 }, colors.map(([colorName, color]) => (React.createElement(Grid.Item, { key: colorName },
    React.createElement(Paper, { style: { padding: '1rem' } },
        React.createElement(ColorRectangle, { color: color }),
        React.createElement(Typography, { size: 'xsmall' }, `${colorGroupName}.${colorName}`),
        React.createElement(Typography, { variant: 'heading', size: 'small' }, color.toUpperCase())))))));
const ColorRectangle = ({ color }) => (React.createElement("div", { style: {
        width: '7rem',
        height: '7rem',
        backgroundColor: color,
    } }));
export default Example;
//# sourceMappingURL=Default.example.js.map