import titleCase from 'ap-style-title-case';
import isString from './is-string';
const toTitleCase = (node) => {
    if (!node || !isString(node)) {
        return node;
    }
    return titleCase(node);
};
export default toTitleCase;
//# sourceMappingURL=to-title-case.js.map