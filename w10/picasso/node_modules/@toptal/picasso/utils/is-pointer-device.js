import { isBrowser } from '@toptal/picasso-shared';
const isPointerDevice = () => {
    return isBrowser()
        ? window.matchMedia('(hover: hover) and (pointer: fine)').matches
        : undefined;
};
export default isPointerDevice;
//# sourceMappingURL=is-pointer-device.js.map