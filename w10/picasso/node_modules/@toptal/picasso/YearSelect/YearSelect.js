var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import React, { useMemo } from 'react';
import Select from '../Select';
import { forwardRef, documentable } from '../utils/forward-ref';
const generateOptions = (from, to) => {
    const length = Math.abs(to - from) + 1;
    return Array.from({ length }).map((_, index) => {
        const value = to < from ? from - index : index + from;
        return {
            value,
            text: value.toString(),
        };
    });
};
export const YearSelect = documentable(forwardRef((_a, ref) => {
    var { from, to } = _a, rest = __rest(_a, ["from", "to"]);
    if (!to || !from) {
        throw new Error(`Invalid range. Please check the values you have passed: from: ${from}, to: ${to}`);
    }
    const options = useMemo(() => generateOptions(from, to), [from, to]);
    return React.createElement(Select, Object.assign({}, rest, { ref: ref, options: options }));
}));
YearSelect.defaultProps = {};
YearSelect.displayName = 'YearSelect';
export default YearSelect;
//# sourceMappingURL=YearSelect.js.map