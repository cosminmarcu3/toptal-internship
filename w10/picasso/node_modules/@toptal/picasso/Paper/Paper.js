var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { makeStyles } from '@material-ui/core/styles';
import { Paper as MUIPaper } from '@material-ui/core';
import React, { forwardRef } from 'react';
import styles from './styles';
const useStyles = makeStyles(styles, { name: 'PicassoPaper' });
export const Paper = forwardRef(function Paper(props, ref) {
    const { className, style, elevation, children, 'data-testid': dataTestId } = props, rest = __rest(props, ["className", "style", "elevation", "children", 'data-testid']);
    const classes = useStyles();
    return (React.createElement(MUIPaper, Object.assign({}, rest, { ref: ref, classes: classes, className: className, style: style, elevation: elevation, "data-testid": dataTestId, square: true }), children));
});
Paper.defaultProps = {
    elevation: 1,
};
Paper.displayName = 'Paper';
export default Paper;
//# sourceMappingURL=Paper.js.map