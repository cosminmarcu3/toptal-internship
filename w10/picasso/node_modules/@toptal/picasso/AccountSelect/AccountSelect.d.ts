import React, { HTMLAttributes } from 'react';
import { BaseProps } from '@toptal/picasso-shared';
declare type Account = {
    /** User's id */
    id: string;
    /** URL of the page the user's item link goes to */
    href?: string;
    /** Full name of the user that is displayed next to the avatar */
    name: string;
    /** Position of the user at the listed company */
    position: string;
    /** Link to user's photo */
    avatar?: string;
};
export interface Props extends BaseProps, Omit<HTMLAttributes<HTMLUListElement>, 'onSelect'> {
    /** List of available accounts */
    accounts: Account[];
    /** Callback invoked when specific role record is clicked in the list */
    onSelect: (account: Account) => void;
}
export declare const AccountSelect: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLUListElement>>;
export default AccountSelect;
